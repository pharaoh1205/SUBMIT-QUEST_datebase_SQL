# 【手順1】問題文から名詞、それらのリレーション関係を書き出す

## ❶名詞を書き出す

```jsx
名詞
1.channels（チャンネル）
2.genres（ジャンル）
3.programs（番組）
4.program_genres（番組ジャンル中間テーブル）
5.seasons（シーズン）
6.episodes（エピソード）
7.channel_schedules（番組枠）
```

<details>
<summary>名詞を紙に書き出したらprogram_genresだけ思いつかなかった  </summary>

紙に書き出しても `program_genres` が出てこないのは当然なんです。
なぜなら、`program_genres` は現実世界にある「モノ（名詞）」ではなくデータベースの都合で生まれた「仕掛け（関係性）」だからです

**💡 中間テーブル（橋渡し役）が必要な理由**

例えば、ノートの1行に以下のように書いたとします。

- **番組**: 鬼滅の刃
- **ジャンル**: アニメ, アクション, ファンタジー

人間ならこれを見れば「なるほど3つのジャンルがあるんだな」と直感で理解できますよね。しかしデータベースは「1つのマス（カラム）には1つの値しか入れられない」というルール（正規化）があります。
1つのマスに「アニメ, アクション, ファンタジー」とカンマ区切りで詰め込むと、後から「アニメジャンルの番組だけ検索したい」となった時にDBが正しく検索できなくなってしまうのです。

**じゃあどうやって紐づける？**
番組テーブルとジャンルテーブルを直接繋ごうとすると、お互いに相手を複数持ってしまう（多対多の関係）ので、どっちのテーブルに相手のIDを書けばいいかパニックになります。そこで、「番組とジャンルのペアだけをメモする専門の紙」を1枚用意します。それが中間テーブルです！

【 program_genres（番組とジャンルのメモ帳）】

| program_id (番組) | genre_id (ジャンル) |
| :--- | :--- |
| 鬼滅の刃 (1) | アニメ (10) |
| 鬼滅の刃 (1) | アクション (11) |
| 鬼滅の刃 (1) | ファンタジー (12) |


こうやって「1行につき1つのペア」で書けば、DBのルールを守ったまま「鬼滅の刃は3つのジャンルを持っている」という事実をカンペキに表現できます。

**🔍 DB設計での名詞の探し方のコツ**

紙に書き出すとき、名詞は大きく分けて3つの種類に分類すると設計が一気にラクになります！

1. **実体（概念・モノ）** ← 最初に見つかる名詞チャンネル、番組、シーズン、エピソード、ジャンル
2. **出来事・ログ（コト）** ← 時間が絡むもの放送枠 / スケジュール（いつ放映されたか）
3. **関係性（ペア）** ← **紙には出てこない！**「多対多」の場所を探して後から足す番組ジャンル（`program_genres`）

名詞を書き出すアプローチ自体は100点満点です！

「多対多の関係（1つの〇〇が複数の△△を持ち、1つの△△に複数の〇〇が含まれる関係）を見つけたら、間にペアメモ用テーブルを挟む」というテクニックだけ覚えておけば、今後どんなDB設計でもバッチリ対応できますよ！
</details>

## ❷リレーション関係を書き出す
<details>
<summary>なぜリレーションを見つけるのか？  </summary>
結論から言うと、見つけたリレーションは次の2つのステップ（開発の現場）で決定的に活きてきます！

### 1. テーブルに「どのカラム（ID）を追加するか」が機械的に決まる

リレーションを見つけると、テーブル同士を繋ぐための「共通のID（外部キー）」をどのテーブルに持たせればいいかが自動的に決まります

```jsx
「1対多」の関係と分かったら
　　➔「多」側のテーブルに「1」側のIDを付け足す！

例: シーズン（1）対 エピソード（多）なら、episodes テーブルに season_id をカラムとして追加
```

```jsx
「多対多」の関係と分かったら
　➔お互いのIDを入れた「中間テーブル」を新しく作る！

例: 番組（多）対 ジャンル（多）なら、program_genres というテーブルを作って program_id と genre_id を入れる。
```

つまり、リレーションを見つける作業は「テーブルのカラム（枠組み）を確定させる作業」そのもの

### 2. アプリの検索・表示機能（SQLのJOIN）で大活躍する

ここが一番の醍醐味です！
アプリで「画面に情報を表示したい」となったとき、リレーションで繋いだIDを使ってデータ同士を**合体（結合 =JOIN）させて引っ張ってくることができます。

💡 実際の活用シーンの例（まさにステップ2課題！）

例えば、アプリの画面に**「『鬼滅の刃』の全エピソード一覧を表示したい」**となったとします。

```jsx
1. programs テーブル から「鬼滅の刃（id = 1）」を探す

2. リレーション（program_id）を辿って `seasons` テーブルから「シーズン1（id = 10）」を見つける

3. リレーション（season_id）を辿って `episodes` テーブルから「season_id = 10 のエピソード（1話〜26話）」を全件取ってくる！
```

この「リレーションを辿ってデータを一気に持ってくる命令」を書くのが、今回の課題のステップ2（SQLの作成）です。

### 🔑 まとめ

リレーションを見つけると、

```jsx
1. DB構築時：迷わず正しいテーブルとカラム（外部キー）が作れる！
2. アプリ開発時：「プログラムから欲しいデータを自由自在に引き出せる（SQLが書ける）」ようになる！
```

というように、設計から開発までの**「設計図」**としてそのまま活きてきます。

</details>


### リレーションを見つける手順、その後にやること

**🔴ステップ1: 書き出した名詞から「2つ」をペアにして並べる**

まずは紙に書いた名詞から、関係がありそうな2つを自由にピックアップします。

- 例: 「番組」と「ジャンル」
- 例: 「シーズン」と「エピソード」
- 例: 「チャンネル」と「番組」

**🔴ステップ2: 【超重要】2つの問いを自分に投げかける(1対多か多対多の判別)**

ペアにした2つの名詞に対して、矢印を往復させて以下の2つの質問をします。

Q1 「A（左）」は「B（右）」を複数持てる？
Q2 逆に「B（右）」は「A（左）」を複数持てる？

この「はい/いいえ」の組み合わせで、機械的に答えが出ます。

**🔴ステップ3: 質問の結果でタイプ（テーブルの作り方）を判定する**

・判定パターンA: 片方だけ「はい」（＝1対多）

```jsx
質問①（番組 ➔ シーズン）: 1つの「番組」にシーズンは複数ある？
 ➔ はい（シーズン1、シーズン2…）

質問②（シーズン ➔ 番組）: 1つの「シーズン」が複数の番組に属することはある？
 ➔ いいえ（『鬼滅の刃 シーズン1』は『鬼滅の刃』だけのもの）
```

💡 判定: 1対多（親子関係）

- 「多」になる側（シーズン）に、「1」側（番組）のIDを1つ書くだけ！
- ➔ 中間テーブルは不要！

ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー

・判定パターンB: 両方とも「はい」（＝多対多）

```jsx
質問①（番組 ➔ ジャンル）: 1つの「番組」にジャンルは複数ある？ ➔ はい（アニメ、アクション…）
質問②（ジャンル ➔ 番組）: 1つの「ジャンル」に番組は複数ある？ ➔ はい（『鬼滅』『SPY×FAMILY』…）
```

💡 判定: 多対多

- お互いのテーブルに書ききれないので、ペアを記録する専用テーブル（中間テーブル）を新しく作る！
- ➔ 中間テーブル（program_genres）が必要！

ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー

・判定パターンC: 両方とも「いいえ」（＝1対1）

- 例: 「ユーザー」と「マイナンバーカード」
- 質問①も②も「いいえ（1つだけ）」になる関係。

💡 判定: 1対1

- 基本的には同じ1つのテーブルにまとめてしまってOK


```jsx
リレーション

●1対多（中間テーブル不要・親のIDを子に持たせる）
	1.●番組(1) ↔ シーズン(多)
	2.●シーズン(1) ↔ エピソード(多)
	3.🟡チャンネル(1) ↔ 番組枠(多)
	4.●エピソード(1) ↔ 番組枠(多)
	
●多対多（中間テーブルが必要）
	1.番組(多) ↔ ジャンル(多)
	中間テーブル: program_genres
	2.🟡チャンネル(多) ↔ エピソード(多)
	中間テーブル: 番組枠（channel_schedules）

```
<br>
補足：番組枠program_genresと（channel_schedules）の2つの中間テーブルの違い

```jsx
今回の設計に出てきた2つの中間テーブルを比べると、役割の違いがよく見えてきます。

1.program_genres（純粋な中間テーブル）
- 役割: 「番組」と「ジャンル」を紐付けるためだけに存在するテーブル。
- 持っているデータ: 基本的に `program_id` と `genre_id` のIDペアがメイン。

2.channel_schedules（情報を持つ中間テーブル）
- 役割: 「チャンネル」と「エピソード」を結びつけつつ、「いつ放送されるか」「何回見られたか」という独自のデータも持つテーブル
- 持っているデータ: `channel_id` と `episode_id` のIDペアに加えて、`start_time`（放送開始時刻）や `view_count`（視聴数）など

🔑 まとめ
実務のデータベース設計でも、ただID同士を繋ぐだけのシンプルなお結び役だけでなく、channel_schedulesのように「IDのペア ＋ その時に発生するデータ（日時や視聴数など）」を一緒に記録する中間テーブル**はものすごく頻繁に使われます！
```

ーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーーー

## 以下作業しているなかでの疑問点等

<details>
<summary>疑問：仕様の階層構造（番組 ➔ シーズン ➔ エピソード ➔ 番組枠）親→子の関係に関して
なんで番組枠が階層の一番下だとわかるのか？  </summary>

結論から言うと「動画というデータが実際にユーザーの画面（テレビ）に流れる、最後の『アウトプット（出来事）』だから」

### 🎬 映画やアニメが作られて放送されるまでの流れ

現実世界でコンテンツ（アニメなど）が作られてから、テレビやABEMAで放送されるまでの流れを想像してみてください。

1. 番組を作る：まず『鬼滅の刃』という企画・作品（番組）が生まれる
2. シーズンに分ける：全何話かを「立志編」「遊郭編」などのまとまり（シーズン）に分ける
3. 動画ファイル（1本）が完成する：編集が終わって「第1話」「第2話」という実際の動画ファイル（エピソード）が納品される
4. 放送枠にセットする：完成した動画ファイルを「ドラマ1チャンネルの 8/1(土) 20:00〜20:30 の枠（番組枠）」にセットして放映する

このように「番組 ➔ シーズン ➔ エピソード」までは『動画コンテンツそのもの（作品）』**を作っている段階
そして最後に「できた動画（エピソード）をいつ・どのチャンネルで流すか」というスケジュール（番組枠）が割り当てられます。

### 💡 「番組枠が一番下」だとわかる決定的な証拠2つ

**証拠①: 番組枠がなくてもエピソードは存在できるが、エピソードがないと番組枠は存在できない**

- 制作会社で「第1話の動画データ（エピソード）」が完成・保存されても、まだテレビで放送されていなければ番組枠は0個
- 一方で、流す動画データ（エピソード）が何も決まっていないのに「番組枠」だけがポツンと存在することはあり得ません

つまり「エピソード」があって初めて「番組枠」が作れる（＝番組枠はエピソードに依存している）**ため、番組枠の方が下の階層になります。

証拠②: 「再放送」があるから
仕様書に「再放送もあるため、ある番組（エピソード）が複数の番組枠で放映される」とありましたよね。

- 1話の動画データ（エピソード）は 1本 しかありません。
- でも、番組枠は 「本放送（月曜20時）」「再放送（土曜14時）」「年末特番枠（12/31）」 と、1本の動画に対して複数（下位）に増えていきます。

1つの親（エピソード）に対して、多の子ども（番組枠）がたくさんぶら下がる形になるので、階層構造としても一番下が「番組枠」になります。
</details>




<details>
<summary>疑問：「チャンネル」と「エピソード」がなぜペアとして成立するのか？？→KPIでのシステム的な関係から必要なため  </summary>
<br>
実は「チャンネル」と「エピソード」は、現実世界では直接結びついていません。
では、なぜデータベース設計の文脈で「ペア」として登場するのか？
その答えは「サービスでやりたいこと（仕様）を実現するために、システムが裏側で関係を作っているから」

### 1. 現実世界の感覚（直感通り）

- チャンネル（例: ドラマ1チャンネル）は「動画を流す放送局/枠組み」
- エピソード（例: 鬼滅の刃 1話）は「完成した1本の動画ファイル」

この2つはただの「別のデータ」であって、お互いのデータの中に相手の情報を直接書き込むことはできません。

- チャンネルの箱に「動画データ」を直接埋め込むことはできない
- 動画データ（ファイル）の中に「どのチャンネルで流すか」を直接書き込むこともできない（だって再放送でいろんなチャンネルで流れるかもしれないから）

つまり「この2つは直接のペアではない」のです。

### 2. でも、アプリの画面ではどう見えているか？

しかし、ユーザーが ABEMA などのアプリを開いた時のことを想像してみてください。
画面にはこう表示されていますよね？

【ドラマ1チャンネルの画面】
➔ 今流れている動画：「鬼滅の刃 第1話」
ユーザーから見ると「このチャンネルで、このエピソード（動画）が流れている」という関係が確かに存在しています。

さらに、仕様書にはこう書かれていました。
「KPIとして、チャンネルの番組枠のエピソードごとに視聴数を記録する」
→つまりエピソードというのは動画単位の最小単位(番組 ➔ シーズン ➔ エピソード)で
その最小単位で視聴数(再生数)としてカウントしたいということ

つまり”システムとしては”「チャンネル A」と「エピソード B」が出会った事実（放送されたこと）を記録して、その時の視聴数を数えたいのです。
→システム的な側面からも考えるべき
</details>




<details>
<summary>疑問：なぜチャンネル(多) ↔ エピソード(多)が多対多になる？→中間テーブルとして番組枠があるから  </summary>

## 💡 なぜ「多対多」になるのか？（2つの視点）

「チャンネル」と「エピソード」の関係を、それぞれの立場から見てみましょう。

### 1. チャンネル視点（1つのチャンネルから見ると？）

- 「アニメ1チャンネル」では、鬼滅の刃 第1話、第2話、呪術廻戦 第1話… と、**複数のエピソード（多）**を放映します。
- ➔ つまり、チャンネル(1) ↔ エピソード(多)

### 2. エピソード視点（1つのエピソードから見ると？）※再放送の概念

- 「鬼滅の刃 第1話」は、アニメ1チャンネルで本放送され、翌週にはドラマ1チャンネルで再放送される… というように複数のチャンネル（多）で放映されることがあります。
- ➔ つまり、エピソード(1) ↔ チャンネル(多)

🔑 だから「多対多」になり、番組枠（中間テーブル）が生まれる！

両方向から見てどちらも「多」になるため、「チャンネル(多) ↔ エピソード(多)」という多対多の関係になります。

データベースでは「多対多」の関係を直接繋ぐことができないルール（※どちらのテーブルにも相手のIDをきれいに持てなくなるため）があるので、その間に「番組枠（channel_schedules）」という中間テーブルを置くことで、2つの「1対多」に分解して整理しているわけです！

```
【チャンネル】(1) ─── (多) 【番組枠（中間テーブル）】 (多) ─── (1) 【エピソード】
```

- 「この番組枠は、どのチャンネルの枠か？」➔ チャンネル(1) ↔ 番組枠(多)
- 「この番組枠で、どのエピソードを流すか？」➔ エピソード(1) ↔ 番組枠(多)
</details>




<details>
<summary>疑問：チャンネル(1) ↔ 番組枠(多)とエピソード(1) ↔ 番組枠(多)が成立している
つまり階層的にはチャンネル→ 番組枠→エピソードとなるはず
そうなると🟡チャンネル(多) ↔ エピソード(多)は「飛び越えて直接繋いではいけない（途中の階層をスキップしてはいけない」というデータベース設計のルールを破っているのではないか？  </summary>

なぜこれが矛盾していないのか、「階層（親子関係）」と「交差（結合関係）」の違いという視点から見ていきます

### 💡 そもそもチャンネル→ 番組枠→エピソードの階層構造が間違っている

📺 なぜ「番組枠 ➔ エピソード」に見えるのか？
テレビ欄やABEMAの画面を見ると、こうなっていますよね。

```
【ドラマ1チャンネル】
 └─ 20:00〜21:00（番組枠）
      └─ 『鬼滅の刃 第1話』（エピソード）
```

これを見ると、確かに チャンネル ➔ 番組枠 ➔ エピソード という順番の階層構造に見えます！

### ⚠️ でも、これをそのままDBの階層にすると困ることが起きる

もし本当に「番組枠 の下に エピソード」という階層構造にしてしまうと、再放送のときに問題が発生します。
例えば、まったく同じ『鬼滅の刃 第1話』を別の時間や別のチャンネルで再放送するとします。

```
【アニメ1チャンネル】
 └─ 8/1 20:00〜（番組枠 A）
      └─ 『鬼滅の刃 第1話』（データ作成）

【ドラマ1チャンネル】
 └─ 8/2 14:00〜（番組枠 B）
      └─ 『鬼滅の刃 第1話』（また同じデータを作成！？）
```

番組枠の下にエピソードをぶら下げてしまうと放送される（番組枠ができる）たびに、同じ「第1話」の動画データを何度も何度も作らなければならなくなります。

これだと、「動画の時間」「タイトル」「詳細」などのデータが重なって無駄になりますし、動画の修正があった時に全部更新しなければならず大変です。

### 💡 だからDBでは「エピソード」が上に立つ！

データベースでは「1つしか存在しない元データ（動画ファイル）」を親（上）にします。

1. 『鬼滅の刃 第1話』という動画（エピソード）は、世界に1つだけ上に存在する。
2. この「第1話」という親から、**「8/1の放送枠」「8/2の再放送枠」という複数の子ども（番組枠）**に矢印が伸びる。

```
        【鬼滅の刃 第1話】（エピソード：1）
               │
    ┌──────────┴──────────┐
    ▼                     ▼
【8/1 20:00の枠】    【8/2 14:00の枠】（番組枠：多）
```

このように
”データとしては”「エピソード(1) ➔ 番組枠(多)」（エピソードが上、番組枠が下）
にする必要があります。

つまり、番組枠は「チャンネル」と「エピソード」の両方から矢印が注ぎ込む一番下（子）の受け皿として配置するのが、データベースの正しい設計になります。
</details>




<details>
<summary>これらの疑問を解消できたら理解できること  </summary>

### 💡 1. 「番組枠」は、2つの独立したグループの「交差点（クロスポイント）」

データベースには、大きく分けて2つの独立したデータのグループ（系列）が存在します。

```jsx
❶【グループA：放送局側】
　⚬ チャンネル（ドラマ1、アニメ1など）

❷【グループB：作品コンテンツ側】
　⚬ 番組（鬼滅の刃）➔ シーズン（シーズン1）➔ エピソード（第1話）
```

「番組 ➔ シーズン ➔ エピソード」は、1本の動画ができるまでの一本道の縦の階層（家系図）です。

そして、グループAの「チャンネル」と、グループBの「エピソード（動画）」が出会って「いつ放送するか」を記録する場所が「番組枠」になります。
２つを繋げようとした場合、どこで出会うのか、一番近くて接点があるのが「番組枠」ということ

```
【グループA：放送局】             【グループB：作品コンテンツ】
 チャンネル (ドラマ1)              番組 (鬼滅の刃)
      │                                │ (1対多)
      │                                ▼
      │                           シーズン (シーズン1)
      │                                │ (1対多)
      │                                ▼
      │                           エピソード (第1話)
      │                                │
      │ (1対多)                         │ （1対多)
      ▼                                ▼
   ┌──────────────────────────────────────┐
   │         番組枠（放映スケジュール）      　  │
   │  (例: 8/1 20:00〜20:30 ドラマ1で1話放送)　│
   └──────────────────────────────────────┘
```

### 💡 2. 2つの親から矢印が集まる「子（中間テーブル）」が番組枠

データの「矢印の向き（親 1 ➔ 子 多）」に注目すると、番組枠の位置づけがハッキリします。

1. チャンネル(1) ➔ 番組枠(多)⚬	1つのチャンネルは、時間帯ごとにたくさんの番組枠を持っている。
2. エピソード(1) ➔ 番組枠(多)⚬	1つのエピソード（動画）は、再放送などでたくさんの番組枠で流される。

つまり、「チャンネル」と「エピソード」という2つの親から見ると、両方にとって「番組枠」は下（子）の位置になる
「チャンネル」と「エピソード」という直接関係のない2つのデータが、一番下の「番組枠」という中間テーブル（合流地点）で結びついているのです。

※これに関しては「チャンネル」と「エピソード」がなぜペアとして成立するのか？？とも関係してくる
→詳細

### 🔑 3. なぜ「階層スキップの禁止ルール」に違反していないのか？

「途中の階層を飛び越えて繋いではいけない」というルールは、一本道の縦のグループ内（番組 ➔ シーズン ➔ エピソード）での話です。

- ❌ NGなスキップの例
「番組」から「シーズン」を飛ばして、直接「エピソード」に繋ぐ。➔ これをやると「シーズン1」などの情報が消えてしまうのでNG。
- ⭕️ 今回の正しい繋がり（スキップしていない）
　⚬グループB内：番組 ➔ シーズン ➔ エピソード と順番通りに繋いでいる。（飛ばしていない！）
　⚬グループA内：チャンネル ➔ 番組枠 と直接繋いでいる。（飛ばしていない！）
　⚬最後に：エピソード ➔ 番組枠 と繋ぐことで、両グループを合流させている。

どの道を見ても階層を飛ばしておらず、順番通りに繋いだ結果として「番組枠」で合流しているため、ルール違反には一切なりません。
</details>

