# 【手順2】各テーブルの「属性（カラム）」を洗い出す

## カラムの作成方法
各テーブルの「属性（カラム）」を洗い出す
それぞれの箱（テーブル）に**「どんな情報を持たせるか」**を書き出します。

⚬「主キー（`id`）」を必ず全テーブルにつける  
⚬「1対多」で特定した**外部キー（相手のID）**を「多」側に持たせる  
⚬「多対多」の２つのキーを外部キーとして中間テーブルに持たせる  
⚬ 仕様書や要件から「タイトル」「時間」「再生数」などの必要な項目を割り振る
